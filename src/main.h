#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>
#include <stdint.h>

extern int filterstep(unsigned char *buffer, int w, int h, unsigned color, char *text, int64_t framecount);
